import keyMirror from '../helpers/keyMirror';

export default keyMirror({
    MOVIES_IS_LOADING: null,
    MOVIES_SUCCESS: null,
    MOVIES_ERROR: null,
    MOVIES_CANCEL: null,
    MOVIES_RESET: null,
});
